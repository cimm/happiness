%h1 Questions

= link_to t('add'), new_question_path

%table
  %tbody
  - @questions.each do |question|
    %tr
      %td= question.body
      %td= question.regularity
      %td= link_to t('edit'), edit_question_path(question)
