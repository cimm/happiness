= render 'layouts/nav'

= form_for @user do |form|

  - if @user.errors.any?
    %ul
      - @user.errors.full_messages.each do |message|
        %li= message

  = render partial: 'form', locals: { f: form }

  %button.button.button-primary{ type: 'submit' }= t('save')
