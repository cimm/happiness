= render 'layouts/nav'

.container
  .clearfix.mb4
    = form_for @user, html: { class: 'mt4' } do |form|

      - if @user.errors.any?
        %ul
          - @user.errors.full_messages.each do |message|
            %li= message

      = render partial: 'form', locals: { f: form }

      .clearfix
        %button.btn.btn-primary.mt1{ type: 'submit' }= t('save')
